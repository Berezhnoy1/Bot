# English Learning Bot 🎓

Telegram бот для определения уровня английского языка и записи на обучение.

## Установка и Запуск

### 1. Установка Python

1. Скачайте и установите Python 3.8 или выше с [официального сайта](https://www.python.org/downloads/)
2. При установке обязательно отметьте галочку "Add Python to PATH"

### 2. Настройка бота

1. Создайте файл `bot.env` в папке с ботом со следующим содержимым:
```env
TOKEN=ваш_токен_бота
ADMIN_ID=ваш_telegram_id
```

2. Получите токен бота:
   - Напишите [@BotFather](https://t.me/BotFather) в Telegram
   - Отправьте команду `/newbot`
   - Следуйте инструкциям для создания бота
   - Скопируйте полученный токен в файл `bot.env`

3. Получите свой Telegram ID:
   - Напишите [@userinfobot](https://t.me/userinfobot) в Telegram
   - Скопируйте полученный ID в файл `bot.env`

### 3. Настройка Google Sheets

1. Создайте проект в [Google Cloud Console](https://console.cloud.google.com/)
2. Включите Google Sheets API
3. Создайте сервисный аккаунт и скачайте его ключ
4. Переименуйте скачанный файл в `google_sheets.json` и поместите в папку с ботом
5. Создайте новую таблицу Google Sheets с названием "Stats"
6. Предоставьте доступ к таблице сервисному аккаунту

### 4. Установка зависимостей

Откройте командную строку в папке с ботом и выполните:
```bash
pip install -r requirements.txt
```

### 5. Запуск бота

#### Windows
Дважды кликните на файл `run_bot.bat`

или

Откройте командную строку в папке с ботом и выполните:
```bash
python main.py
```

## Структура проекта

```
Bot/
├── config.py           # Конфигурация бота
├── database.py         # Работа с Google Sheets
├── keyboards.py        # Клавиатуры
├── questions.py        # База вопросов
├── main.py            # Основной файл
├── requirements.txt    # Зависимости
├── bot.env            # Переменные окружения
├── google_sheets.json  # Ключ сервисного аккаунта
├── handlers/          # Обработчики команд
│   ├── start.py
│   ├── survey.py
│   └── test.py
└── utils/            # Утилиты
    ├── logger.py
    └── states.py
```

## Возможные проблемы

1. **Ошибка "ModuleNotFoundError"**
   - Убедитесь, что все зависимости установлены
   - Попробуйте переустановить зависимости: `pip install -r requirements.txt`

2. **Ошибка "Invalid token"**
   - Проверьте правильность токена в файле `bot.env`
   - Убедитесь, что файл `bot.env` находится в той же папке, что и `main.py`

3. **Ошибка с Google Sheets**
   - Проверьте наличие файла `google_sheets.json`
   - Убедитесь, что сервисный аккаунт имеет доступ к таблице
   - Проверьте, что таблица называется "Stats"

## Поддержка

При возникновении проблем:
1. Проверьте файл `bot_log.log` для получения информации об ошибках
2. Убедитесь, что все файлы настроек присутствуют и правильно заполнены
3. Попробуйте перезапустить бота
